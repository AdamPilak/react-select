Select is an application where you can use the reusable component Select. It can be used with one option or multiple to choose.

Setup:
1. Clone this repository
2. Type "npm run dev" in your project's directory terminal.
